// import { OGMap, OGMapOption } from "@opengis/map";

// import { AttributesEditorComponent } from "../../core/components/attributes-editor/attributes-editor.component";

describe("testing attributes-editor", () => {
    test("initialize new instance", () => {
        //         expect(new AttributesEditorComponent(new OGMap({} as OGMapOption))).toBeInstanceOf(AttributesEditorComponent);
    });
});